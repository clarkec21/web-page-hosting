# jira-fetch.yml — DISABLED
# Replaced by browser bookmarklet approach (no Jira API token required).
# Kept here in case the server-side approach is revisited.
# To re-enable: restore the schedule trigger and switch runs-on back to ubuntu-latest.

name: Fetch Jira Sprint Data (disabled)

on:
  workflow_dispatch:   # manual only — schedule removed

jobs:
  fetch:
    runs-on: self-hosted
    if: false   # prevents accidental manual runs
    permissions:
      contents: write
    defaults:
      run:
        shell: bash
    steps:
      - run: echo "Disabled — use the browser bookmarklet instead."
